<article id="post">
  <header>
    <h1><a href="{{ page.url }}">{{ page.title }}</a></h1>
  </header>

  <p class="meta">
    <time datetime="{{ page.date }}" pudate>{{ page.date | date_to_long_string }}</time>
    {% if page.location %}{{ page.location }}{% endif %}
  </p>
  
  {{ content }}
  
</article>
